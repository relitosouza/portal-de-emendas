services:
  redis:
    image: redis:7-alpine
    container_name: portal-emendas-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --requirepass portal_redis_password

volumes:
  redis_data:
    driver: local
